c = @import("c");

Buf = struct {
    data: Array(char, 32),
};

main = fn() void {
    fmt: Array(char, 8) = ['L', 'v', ' ', '%', 'd', '\0', '\0', '\0'];
    mut buf: Buf = @zero(Buf);
    c.snprintf(&buf.data[0], 32, &fmt[0], 7);
};

@package("enet_pkg");

enet = @import("vendor:enet");

main = fn() i32 {
    if(enet.Initialize() != 0) {
        return 1;
    }

    version: u32 = enet.LinkedVersion();
    if(version == 0) {
        enet.Deinitialize();
        return 2;
    }

    enet.Deinitialize();
    return 0;
};

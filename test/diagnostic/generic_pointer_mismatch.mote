g = @import("../generic/support_factories");

main = fn() i32 {
    mut values: Array(i32, 2) = [1, 2];
    xs: []i32 = @slice(i32, &mut values[0], 2);
    ptr: *mut i32 = g.ptr_pass(i32, xs);
    return *ptr;
};

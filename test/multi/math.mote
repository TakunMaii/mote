pub add :: fn(a: i32, b: i32) i32 {
    return a + b;
};

pub State :: enum {
    idle,
    running,
};

pub Vec2 :: struct {
    x: i32,
    y: i32,

    new: fn(x: i32, y: i32) Vec2 {
        return Vec2 {
            .x = x,
            .y = y,
        };
    },
};
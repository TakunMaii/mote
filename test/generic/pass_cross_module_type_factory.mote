t :: @import("./support_public_types");

main :: fn() i32 {
    value: t.PairType(i32) = t.make_pair(i32, 4, 5);

    if(value.left == 4 && value.right == 5) {
        return 0;
    }
    return 1;
};
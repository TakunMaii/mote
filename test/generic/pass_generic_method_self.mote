g :: @import("./support_factories");

main :: fn() i32 {
    a: g.Vec2(i32) = g.make_vec2(i32, 1, 2);
    b: g.Vec2(i32) = g.make_vec2(i32, 3, 4);
    g.add_vec2(i32, &a, b);

    if(a.x == 4 && a.y == 6) {
        return 0;
    }
    return 1;
};
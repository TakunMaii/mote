g :: @import("./support_factories");

main :: fn() i32 {
    values: Array(g.Pair(i32), 2) = [
        g.make_pair(i32, 1, 2),
        g.make_pair(i32, 3, 4),
    ];
    xs: []g.Pair(i32) = @slice(g.Pair(i32), &values[0], 2);
    if(xs[1].right == 4) {
        return 0;
    }
    return 1;
};
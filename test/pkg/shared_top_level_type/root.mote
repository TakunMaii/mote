@package("shared_top_level_type");

pub main = fn() void {
    item: Pair = Pair {
        .left = 1,
        .right = 2,
    };

    left: i32 = item.left;
    right: i32 = item.right;
    if(left == 99 || right == 99) {
        return;
    }
};

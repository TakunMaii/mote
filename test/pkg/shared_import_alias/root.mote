@package("shared_import_alias");

util = @import("util");

pub main = fn() void {
    pair: util.Pair = make_pair(3, 4);
    left: i32 = pair.left;
    right: i32 = pair.right;
    if(left == 99 || right == 99) {
        return;
    }
};

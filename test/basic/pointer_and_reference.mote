
x: i32 = 2;
x_ptr: *i32 = &x;// 指针类型和取址
x_mut_ptr: *i32 = &x;// 指针指向的内容可以通过解引用修改
// 但是同时x_mut_ptr本身又是不可变变量，这就是说让x_mut_ptr指向新的地址是不可行的，
// 只是说改变其指向的位置的值是可行的

x_value := *x_ptr;//取值，不管可不可变都能取值
x_ref: &i32 = x;//引用类型
x_mut_ref: &i32 = x;// 当前只有一种引用类型，赋值到引用会写回原值
*x_mut_ptr = 5;
x_mut_ref = 6;

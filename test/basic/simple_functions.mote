
//函数的声明，本质上和变量的声明是同一个语法（相当于把函数看作一个表达式，借鉴zig的做法）
//fn关键字表示这是一个函数的声明
//x是引用参数，y是值参数
addYToX :: fn(x: &i32, y: i32) void {//void是返回值类型
    x = x + y;//这一步能实现依赖于x是可变引用
};//这也意味着这里一定要有分号

x := 3;
y :: 4;
addYToX(x, y);//如果x不是mut，这一步就将报错
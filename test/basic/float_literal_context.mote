Vec2 :: struct {
    x: f32,
    y: f32,
};

v: Vec2 = Vec2 {.x = -0.5, .y = 0.25};
xs: Array(f32, 3) = [-1.0, 0.0, 1.0];

main :: fn() i32 {
    if(v.x < 0.0 && xs[2] > 0.5) {
        return 0;
    }
    return 1;
};
rl = @import("vendor/raylib");

mut frames: i32 = 0;

rl.InitWindow(320, 240, "raylib loop");
rl.SetTargetFPS(60);

while(!rl.WindowShouldClose() && frames < 2) {
    rl.BeginDrawing();
    rl.ClearBackground(rl.RAYWHITE);
    rl.EndDrawing();
    frames = frames + 1;
}

rl.CloseWindow();
